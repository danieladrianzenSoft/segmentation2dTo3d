"""Workflow registry for controlling which workflows are exposed externally."""

# Add workflow module names (without .py) that should be exposed in public mode.
PUBLIC_WORKFLOWS = [
    "unite_meshes",
]
